{
  "buildCommand": "cd spark-realm (1) && npm run build",
  "outputDirectory": "spark-realm (1)/dist",
  "framework": "vite",
  "installCommand": "cd spark-realm (1) && npm install",
  "devCommand": "cd spark-realm (1) && npm run dev",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        {
          "key": "X-Content-Type-Options",
          "value": "nosniff"
        },
        {
          "key": "X-Frame-Options",
          "value": "DENY"
        },
        {
          "key": "X-XSS-Protection",
          "value": "1; mode=block"
        }
      ]
    }
  ]
} 